{
"type": "String",
"name": "{{thingid}}_{{item_obj.name}}",          {# must match "linkedItems" in channel definition (from thing) #}
"label": "{{item_obj.label}}",
"category": "{{item_obj.icon}}",
"tags": [
  "{{item_obj.tags}}"
],
"groupNames": {{item_obj.groups.split(',')}},
},

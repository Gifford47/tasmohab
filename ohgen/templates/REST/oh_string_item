{
"type": "String",
"name": "{{thingid}}_{{item_obj.name}}_{{item_obj.label}}",          {# must match "linkeditem_objs" in channel definition (from thing) #}
"label": "{{item_obj.label}}",
"category": "{{item_obj.icon}}",
"tags": [
  "{{item_obj.tags}}"
],
"groupNames": {{item_obj.groups.split(',')}},
},

{
"type": "Rollershutter",
"name": "{{thingid}}_{{item_obj.name}}_{{for_loop.index}}",               {# must match "linkeditem_objs" in channel definition (from thing) #}
"label": "{{item_obj.label}}",
"category": "{{item_obj.icon}}",
"tags": [
  "{{item_obj.tags}}"
],
"groupNames": {{item_obj.groups.split(',')}},
},
